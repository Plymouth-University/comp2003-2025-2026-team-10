# CFD Visualisation Pipeline — Final Version

**COMP2003 Team 10 | University of Plymouth | 2025–2026**

This folder contains the complete, consolidated pipeline that converts OpenFOAM CFD simulation data into a real-time interactive 3D visualisation in Unity. It replaces all individual team member pipelines with a single two-command workflow.

---

## What this pipeline does

The simulation produces ~250 GB of raw OpenFOAM data. This pipeline extracts three data streams from it and delivers them to Unity:

| Component | Source data | Output |
|---|---|---|
| Water surface | `alpha.water` isosurface (CFD) | 256×256 heightmap PNGs, lerped each frame |
| Wind streamlines | Velocity field `U` streamlines (CFD) | CSV files loaded into Unity particle system |
| Turbine motion | Hub wind speed + platform displacement (CFD) | CSV with surge, heave, pitch per timestep |

---

## Folder structure

```
final version/
├── App/                        ← Option A: Pre-built standalone exe
│   └── HOW_TO_BUILD.md         ← How to build and package the exe from Unity
│   (CFD_Visualisation.exe)     ← Goes here after building — zip and send to client
│   (CFD_Visualisation_Data/)   ← Generated by Unity build — must stay next to exe
│
└── Pipeline/                   ← Option B: Full pipeline (open Unity Project to edit)
    ├── Unity Project/          ← Open this in Unity Hub
    │   └── Assets/
    │       ├── Scripts/        All 5 C# scripts
    │       ├── source/         Wind Turbine FBX (David)
    │       ├── textures/       PBR texture maps
    │       ├── Materials/      trail.mat (Oscar)
    │       ├── Prefabs/        Wind.prefab (Oscar)
    │       ├── Resources/
    │       │   └── Streamline/ ← Drop 35 wind CSVs here (or run run_pipeline.py)
    │       └── StreamingAssets/
    │           ├── HeightMaps/ ← Drop 35 heightmap PNGs here (or run run_pipeline.py)
    │           └── Turbine/    ← Drop turbine.csv here (or run run_pipeline.py)
    ├── Simulation_Input/
    │   ├── water/              ← Drop wave_N.csv files here, then run run_pipeline.py
    │   ├── wind/               ← Drop N.csv wind files here, then run run_pipeline.py
    │   └── turbine/            ← Drop turbine.csv here, then run run_pipeline.py
    ├── run_pipeline.py         ← Run this to process CSVs and push data into Unity Project
    ├── extract_all.py          ← Run with pvpython to extract fresh data from OpenFOAM
    ├── process_water.py
    ├── process_wind.py
    └── process_turbine.py
```

---

## Client workflow

### First time (one-off setup)
1. Install Python 3.10+, run `pip install numpy Pillow scipy`
2. Install Unity 2022 LTS
3. Install ParaView 5.11+ (only needed to re-extract from OpenFOAM)
4. Set up the Unity scene (see Unity Setup below)

### Every time new simulation data is loaded

**Step 1 — Extract from OpenFOAM** *(skip if you already have CSVs)*
```
pvpython extract_all.py
```
Edit `CASE_FILE` at the top of `extract_all.py` to point to your `.foam` file first.

**Step 2 — Process and push to Unity**
```
cd FOWT-Vis-Pipeline/Pipeline
python run_pipeline.py
```
Edit `UNITY_PROJECT` at the top of `run_pipeline.py` to point to your Unity project folder first.

**Step 3 — Play**
Open Unity and press Play. All three components animate automatically.

---

## Unity scene setup (one-time)

Add all scripts from `Unity Scripts/` to your Unity project, then:

| Script | Attach to | Set in Inspector |
|---|---|---|
| `HeightMapWaterPlane.cs` | Empty GameObject (water plane) | `planeSize` to match domain |
| `PlaneGenerator.cs` | Same GameObject as above | — |
| `CombinedStreamlineSystem.cs` | Empty GameObject | `particlePrefab`, `particleParent` |
| `FOWTController.cs` | FOWT root GameObject | `platformTransform`, `bladeTransform` |
| `BobbingController.cs` | Same as FOWTController | `bobFrequency`, `bobAmplitude` |

Scene positions are hardcoded in each script to match the simulation domain and require no manual alignment:
- Water plane → `(7.5, 0, 0)` — centre of the 0–15 m simulation domain
- FOWT → `(7.5, 2.135, 0)` — hub height from CFD

---

## How each script works

### `extract_all.py`
Runs inside ParaView's bundled Python interpreter (`pvpython`). Opens the OpenFOAM case once and runs three extraction pipelines per timestep:
- **Water**: Contour filter at `alpha.water = 0.5` → CSV of surface point coordinates
- **Wind**: StreamTracer on velocity field `U` → CSV with `RegionId, x, y, z` per point
- **Turbine**: Hub probe for wind speed + platform point displacement → scalars per timestep

All output goes to `Simulation_Input/`.

### `run_pipeline.py`
Orchestrates the three processing modules and writes output directly into the Unity project:
- `process_water.py` → bins point cloud to 256×256 grid, normalises, Gaussian blur, 16-bit PNG
- `process_wind.py` → clears old CSVs from `Resources/Streamline/`, copies renamed wind CSVs
- `process_turbine.py` → validates column structure, copies to `StreamingAssets/Turbine/`

### `HeightMapWaterPlane.cs`
Generates a procedural plane mesh at runtime and deforms it each frame by sampling two heightmap PNGs and lerping between them. Starts from frame 1 (not frame 0) to avoid CFD startup transient artifacts. Supports a `bobbingOffset` float that `BobbingController.cs` writes to each frame.

### `CombinedStreamlineSystem.cs`
Loads all wind CSVs from `Resources/Streamline/` at startup. Reconstructs full streamlines by combining backward and forward integration halves. Uses arc-length parametrisation and Catmull-Rom spline interpolation for smooth constant-speed particle flow. Cycles through timestep files every `fileSwitchInterval` seconds.

### `FOWTController.cs`
Reads `turbine.csv` from `StreamingAssets/Turbine/` at startup (no Inspector drag-and-drop required). Lerps platform position and pitch between snapshots. Rotates blades at a speed proportional to wind speed × tip-speed ratio.

---

## Dependencies

| Tool | Version | Purpose |
|---|---|---|
| Python | 3.10+ | Processing pipeline |
| numpy | any | Array maths in `process_water.py` |
| Pillow | any | PNG export |
| scipy | any | Gaussian blur |
| ParaView | 5.11+ | CFD data extraction (`extract_all.py` only) |
| Unity | 2022 LTS+ | Visualisation |

---
