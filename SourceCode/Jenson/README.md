--Temporal Interpolation prototype--

This is a proof of concept prototype for reducing CFD dataset size by storing sparse temporal keyframes.

FILES:

TemporalInterpolationUnity.cs

export_temporal_interpolation.py

temporal_interpolation.py

temporal_interpolation_example.json

## Run (Python)

From this folder:

```bash
python export_temporal_interpolation.py
python temporal_interpolation.py
```
